import React from 'react';

const FlightResults = (props) => {
  
  console.log(props);

  return (
    <div className="flex">
      Flight results here
    </div>
  );
};

export default FlightResults;
